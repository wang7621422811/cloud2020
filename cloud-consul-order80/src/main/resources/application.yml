server:
  port: 80


spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host:  47.93.237.204
      port: 8500
      discovery:
        service-name: ${spring.application.name}
